# Custom Grafana Panel

This directory is reserved for the custom Grafana panel plugin source code.

Keep the plugin implementation here, separate from `grafana/`, which contains
dashboard JSON and Grafana provisioning configuration. The panel will be built
and packaged independently from the Go exporter.
